"""Enterprise Vibe FGA authorization layer.

Importable as ``authz.enforcement`` once installed (the package is included in
pyproject's setuptools find list). Contains the OpenFGA model modules
(``authz/model``), the enforcement library (``authz/enforcement``), the
governed admin API, seeds, tests, and the cross-platform bootstrap.
"""
